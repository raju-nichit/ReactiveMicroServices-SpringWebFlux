import com.fasterxml.jackson.databind.ObjectMapper;
import java.util.Map;

String jsonString = """
{
  "$query": {
    "$and": [
      { "json.templateName": { "$regex": "(?i).*TEMPLATE_TESTI.*" } },
      { "json.templateId": "TEMP123" }
    ]
  }
}
""";

ObjectMapper mapper = new ObjectMapper();
Map<String, Object> queryMap = mapper.readValue(jsonString, Map.class);
