<session-config>
        <cookie-config>
            <http-only>true</http-only> <!-- Prevents JavaScript access to cookies -->
            <secure>true</secure>       <!-- Ensures cookies are sent over HTTPS -->
        </cookie-config>
        <session-timeout>30</session-timeout> <!-- Set session timeout in minutes -->
    </session-config>
