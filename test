 String response = webClient.get()
                .uri("https://jsonplaceholder.typicode.com/posts/1") // Replace with your API URL
                .retrieve()
                .bodyToMono(String.class) // Parse response as a String
                .block(); // Block for synchronous response

        // Print response
        System.out.println(response);
